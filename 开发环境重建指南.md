# VSCode 嵌入式开发环境重建指南（51 → STM32 · 新机器版）

> **用途**：新机器上按此文档逐步重建环境。所有配置给**完整带注释**的原文，可直接复制；
> 可选项做成"取消注释即启用"的形式。**原则**：先装工具 → 再配配置 → 最后验证；
> 配置写全局（Settings Sync 同步），项目级只用 `.clangd`（随 Git 走）；引擎只用 clangd。

---

## 第 0 步：机器级工具（一次性）

### Git
- **原因**：项目的 `.clangd`、`tasks.json`、`.eide/`、源码都靠 Git 同步（Settings Sync 只管 VSCode 配置，不管项目文件）；学习路线第一要求"Git 提交成为肌肉记忆"。
- **不做会怎样**：换机器时项目配置要重新手写；代码没有版本管理，改坏了无法回退。
- **操作**：
  ```powershell
  winget install Git.Git
  ```

### Python
- **原因**：一键烧录工具 **stcgal 是 Python 程序**（EIDE 内置 STC 烧录器依赖它）；Python 也是你大二上位机的第二语言。
- **不做会怎样**：EIDE 烧录器选了 stcgal 也跑不起来（报找不到 stcgal）；只能退回手动开 STC-ISP。
- **操作**：
  ```powershell
  winget install Python.Python.3.12
  ```
- **注意**：装完**必须关掉终端重开**，否则 PATH 不刷新，`python` 命令仍找不到。

### 必做：放行 PowerShell 脚本（只执行一次）

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

- **原因**：VSCode 终端默认是 PowerShell，而 Windows 默认执行策略是 `Restricted`
  （禁止运行任何 .ps1 脚本）——cmd 没有这个限制，所以"cmd 能跑、VSCode 终端不能跑"。
  工具包的 .bat 自带 `-ExecutionPolicy Bypass` 所以双击正常，但直接在 VSCode 终端
  运行 .ps1 会被拦。
- **RemoteSigned 的含义**：本地脚本放行、网上下载的脚本仍需签名——安全且官方推荐，
  不需要管理员权限（只改当前用户）。
- **验证**：`Get-ExecutionPolicy` 应显示 `RemoteSigned`；然后 VSCode 终端里
  `.\scripts\4-verify.ps1` 可直接运行。

---

## 第 0.5 步：安装 Keil 双版本（C51 + MDK 共存，零基础版）

**为什么有两个 Keil**：Keil 分两个产品——**C51 版**（编译 51 单片机，程序是 `C51.exe`）和
**MDK 版**（编译 STM32/ARM，程序是 `armclang.exe`）。EIDE 按工程类型自动选对应编译器，
两套同时安装、互不干扰，51 和 STM32 都不用换软件。

**两条铁律（必须遵守）**：

1. **两个版本必须装到不同的目录**！例如 C51 装 `D:\Keil5\C51`，MDK 装 `D:\Keil5\MDK`。
   ⚠️ 都装默认目录 `C:\Keil_v5` 的话，**后装的会覆盖先装的**——结果某个工具链神秘"消失"。
2. **许可证分别激活**：C51 一个许可证，MDK 一个许可证，互不通用。

**零基础安装步骤**：

1. 获取安装包：C51 版（如 `C51V960.exe`）和 MDK 版（如 `MDK539.exe`），
   来源：keil.com 官网或课程资料包
2. 双击 C51 安装包 → 安装路径改成 `D:\Keil5\C51`（目录不存在会自动创建）→ 一路"下一步"
3. 双击 MDK 安装包 → 安装路径改成 `D:\Keil5\MDK` → 一路"下一步"
   （MDK 首次运行可能提示安装 Pack 支持包，选 F103 系列即可，可跳过）
4. 激活：`File → License Management` → 分别输入 C51 和 MDK 的许可证（注册码或 LSK 文件）
5. **验证共存成功**：`D:\Keil5\C51\TOOLS.INI` 和 `D:\Keil5\MDK\TOOLS.INI` 两个文件都存在

**EIDE 侧全自动**：装完运行工具包 `2-自动配置.bat`——**已知 Keil 目录可直接输入根目录跳过搜索**，
不知道就让脚本自动检测；脚本一站式写入 51+STM32 全部 EIDE 设置
（C51/ARM/AC5/OpenOCD/STLink/JLink/Cppcheck 等），未安装的工具会给出说明和安装命令。
检测不到时（装了非默认目录）才需要手动在设置里填 `TOOLS.INI` 路径。

> 常见问题：装完发现 51 能编译但 STM32 不行（或反之）→ 多半是两个装到了同一目录互相覆盖，
> 重装到独立目录即可；或者 TOOLS.INI 路径没对上（用 2-自动配置.bat 重新检测）。

---

## 第 1 步：安装扩展（13 个）

- **原因**：EIDE=编译烧录中枢；clangd=唯一智能引擎；serial-monitor=串口调试；
  errorlens=错误行内显示；path-intellisense=路径补全；comment-translate=英文注释翻译；
  git-graph=Git 可视化；project-manager=多工程一键切换；其余为主题/图标/中文/Python/Markdown。
- **不做会怎样**：缺哪个哪个功能就没有（例如没有 serial-monitor 就无法在 VSCode 里看串口）。
- **操作**（⚠️ Windows 上一次传多个扩展**只装第一个**，必须循环逐条装）：
  ```powershell
  $exts = @(
    "cl.eide", "llvm-vs-code-extensions.vscode-clangd", "ms-vscode.vscode-serial-monitor",
    "ms-ceintl.vscode-language-pack-zh-hans", "zhuangtongfa.material-theme", "pkief.material-icon-theme",
    "usernamehw.errorlens", "christian-kohler.path-intellisense", "intellsmi.comment-translate",
    "mhutchie.git-graph", "ms-python.python", "alefragnani.project-manager", "bierner.markdown-preview-github-styles"
  )
  foreach ($e in $exts) { code --install-extension $e }
  ```
  装完 `code --list-extensions` 核对，然后**重启 VSCode**。

### 每个插件的作用与可能用到的设置

| 插件 | 作用 | 可能用到的设置（VSCode 设置里搜） |
|---|---|---|
| `cl.eide` | 嵌入式工程管理：编译（Keil C51/AC6/SDCC/GCC）、一键烧录、导入导出 Keil 工程 | `EIDE.DisplayLanguage`（中文界面）；`EIDE.C51.INI.Path` / `EIDE.ARM.*`（工具链路径，见第 2/7 步）；`EIDE.Option.EnableClangdConfigGenerator` 保持 false |
| `vscode-clangd` | 唯一代码引擎：补全/跳转/诊断/重命名 | `clangd.arguments`（第 2 步）；`clangd.path`（指定 clangd 本体，默认自动下载）；`clangd.fallbackFlags`（无 compile_commands 且无 .clangd 时才用，我们不依赖） |
| `vscode-serial-monitor` | 串口监视器（51 调试主力） | 无设置项，全部在面板操作：选端口/波特率 |
| `vscode-language-pack-zh-hans` | 界面中文化 | 无 |
| `zhuangtongfa.material-theme` | One Dark Pro 主题 | `workbench.colorTheme` |
| `pkief.material-icon-theme` | 文件图标 | `workbench.iconTheme` |
| `usernamehw.errorlens` | 错误/警告行内显示 | `errorLens.enabled`；`errorLens.fontSize`；`errorLens.decorationsEnabled`（关掉可只在状态栏显示） |
| `christian-kohler.path-intellisense` | `#include` 路径补全 | 无 |
| `intellsmi.comment-translate` | 划词/悬停翻译英文注释 | `commentTranslate.targetLanguage`（默认 zh-CN）；`commentTranslate.hover.enabled` |
| `mhutchie.git-graph` | Git 提交历史可视化 | 默认即可（`git-graph.maxDepthOfRepoSearch` 可调仓库搜索深度） |
| `ms-python.python` | Python 解释器/运行/调试 | `python.defaultInterpreterPath`；`python.terminal.activateEnvInCurrentTerminal` |
| `alefragnani.project-manager` | 多工程一键切换收藏夹 | `projectManager.sortList`（按名称/路径/最近）；`projectManager.autoDetect`（默认自动收录打开过的文件夹） |
| `bierner.markdown-preview-github-styles` | Markdown 预览套 GitHub 样式（唯一保留的 Markdown 扩展） | 无设置，纯样式；预览用 `Ctrl+Shift+V` |

> Markdown 编辑策略：不装 all-in-one 类扩展，用 VSCode 原生编辑 + GitHub 样式预览即可——功能够用且不占资源。

### 明确不装的（装了反而有害）
| 扩展 | 原因 |
|---|---|
| `ms-vscode.cpptools` | 引擎已用 clangd，装它俩抢 C/C++ 语言服务 |
| 空格显示类插件（如 Whitespace+） | 内置 `editor.renderWhitespace` 更好看，插件默认不启动还多一层渲染 |
| `mcu-debug.mcu-debug` | 市场已下架，装不了 |

---

## 第 2 步：全局用户设置（settings.json + keybindings.json）

- **原因**：所有项目通用的一份配置；Settings Sync 会把它同步到任何新机器。
- **不做会怎样**：编码乱码（GBK/UTF-8 混排）、C 文件被自动格式化损坏中文注释、每次换主题/字体。
- **操作**：`Ctrl+Shift+P` → `Preferences: Open User Settings (JSON)` → **整体替换**为：

```jsonc
{
  // ========== 外观 ==========
  "workbench.colorTheme": "One Dark Pro Night Flat",  // 主题（装 One Dark Pro 扩展后可用）
  "workbench.iconTheme": "material-icon-theme",       // 文件图标
  "editor.fontFamily": "JetBrains Mono, Cascadia Code, Consolas, monospace", // 依次回退
  "editor.fontSize": 14,
  "editor.mouseWheelZoom": true,        // Ctrl+滚轮缩放，演示救急
  "editor.semanticHighlighting.enabled": true,  // 类型/变量/函数/宏分色（clangd 供语义信息）
  // 想要连字（!= → ≠ 样式）时取消注释：
  // "editor.fontLigatures": true,

  // ========== 编码 ==========
  // 打开文件自动识别编码：GBK 的 51 源码正常显示，UTF-8 的 Python/MD 不受影响
  "files.autoGuessEncoding": true,
  // ⚠️ 不要全局写死 "files.encoding": "gbk"——那会让所有 UTF-8 工程乱码
  // 个别文件识别错时，点右下角状态栏编码名手动切换

  // ========== clangd ==========
  // 只放 clangd 自己的启动参数（实测 -D/-I 编译参数放这里无效，必须在 .clangd/config.yaml）
  "clangd.arguments": [
    "--header-insertion=never",  // 补全时不自动插入 #include（51 易插错）
    "--background-index"         // 后台索引提速
  ],
  // clangd 本体路径（可选）：默认扩展自动下载；写死可跳过下载，换机器后删掉此项
  // "clangd.path": "C:\\Users\\<用户名>\\AppData\\Roaming\\Code\\User\\globalStorage\\llvm-vs-code-extensions.vscode-clangd\\install\\<版本>\\clangd_<版本>\\bin\\clangd.exe",

  // ========== C 文件格式化 ==========
  // 两个开关都开：保存时格式化 + 输入时格式化（打 ; 或 } 后自动排版，像 Visual Studio）
  // 格式化器 = clangd 自带的 clang-format（微软 C/C++ 扩展底层也是它）
  // 风格由工程根目录的 .clang-format 控制（模板已附 = Visual Studio 风格：4 空格缩进 + 大括号另起一行，
  // 与微软扩展默认的 clang_format_fallbackStyle 一致——所以看起来就是 VSCode 的默认格式）
  // 前提：源码已转 UTF-8 无 BOM——格式化只调空白、不碰编码，不会损坏中文注释
  // ⚠️ 这两个开关存在 VSCode 用户设置里（Settings Sync 会同步）；风格 .clang-format 是项目文件（走 Git）
  "[c]": {
    "editor.formatOnSave": true,
    "editor.formatOnType": true     // 打 ; 或 } 后自动格式化（VS 同款体验）；觉得打断打字就改回 false
  },
  // Python 想保存自动格式化时取消注释（需先 pip install black）：
  // "[python]": { "editor.formatOnSave": true },

  // ========== EIDE ==========
  "EIDE.C51.INI.Path": "D:\\Keil5\\C51\\UV4\\UV4.exe",  // Keil C51 位置（TOOLS.INI 或 UV4.exe 都认）；换路径改这里
  "EIDE.DisplayLanguage": "zh-cn",                        // 中文界面
  "EIDE.Option.EnableClangdConfigGenerator": false,       // ⚠️ 别开：EIDE 自动生成的 .clangd 会覆盖手写的
  // 想用 cppcheck 静态检查时取消注释（先 winget install cppcheck）：
  // "EIDE.Cppcheck.ExecutablePath": "C:\\Program Files\\Cppcheck\\cppcheck.exe",

  // ========== 杂项 ==========
  "chat.disableAIFeatures": true,       // 关 VSCode 内置 AI（想用 Copilot 时改回 false）
  "editor.renderWhitespace": "all",     // 空格显示：空格=点、Tab=箭头
                                        // ⚠️ 1.133 起叫 renderWhitespace，旧名 showWhitespace 已移除
                                        // 只显示行尾空格：改成 "trailing"；关闭：改成 "none"
  "workbench.list.openMode": "doubleClick",   // 文件：单击选中，双击打开（默认单击即开）
  "workbench.tree.expandMode": "doubleClick", // 文件夹：单击选中，双击展开/折叠

  // ========== STM32 阶段（大二）预配置：现在配好无副作用，需要时取消注释 ==========
  // 推荐：直接运行工具包 2-自动配置.bat——一站式写入 51+STM32 全部 EIDE 设置，
  //       未安装的工具（SDCC/OpenOCD/JLink/Cppcheck 等）会自动给出说明
  // "EIDE.ARM.INI.Path": "D:\\Keil5\\MDK\\TOOLS.INI",
  // "EIDE.ARM.ARMCC6.InstallDirectory": "D:\\Keil5\\MDK\\ARM\\ARMCLANG",
  // "EIDE.ARM.GCC.InstallDirectory": "C:\\...\\Arm GNU Toolchain arm-none-eabi\\<版本>"（可选，未装留空）
}
```

快捷键（`Ctrl+Shift+P` → `Preferences: Open Keyboard Shortcuts (JSON)`，`[]` 里加入）：

```jsonc
[
  // 手动格式化快捷键（默认 Shift+Alt+F 仍可用，这条只是新增习惯键）
  {
    "key": "ctrl+alt+l",
    "command": "editor.action.formatDocument",
    "when": "editorTextFocus"
  }
]
```

---

## 第 3 步：clangd 配置（工程 `.clangd` 为主，全局配置已可选）

### 为什么会有这几层

| 层 | 放什么 | 现在还需要吗 |
|---|---|---|
| 全局 config.yaml | C51 宏 + Keil 路径 + 抑制清单 | **可选（兜底）**：每个工程已有自足的 `.clangd`，全局配置不再必需；只在"工程忘了放 `.clangd`"时兜底 |
| 项目 .clangd | 宏翻译 + `-I` 路径 + 编译数据库位置 + 抑制清单 | **必需**：每个工程目录结构不同，必须项目级（内容见 3.2，完整版已内嵌） |
| 编码（UTF-8 无 BOM） | 源码本身 | **必需**：clangd 只认 UTF-8；Keil 不认 BOM |

**底层原因**：EIDE 导出的 `compile_commands.json` 里的头文件搜索路径是 **Keil 专有 `INCDIR(...)` 语法**，
clangd 读不懂 → 头文件全找不到 → 报错成片。所以必须用 `-I` 显式给出路径。

### 3.1 全局配置（可选兜底，已不需要）

**结论**：现在每个工程都有一份自足的 `.clangd`（宏、路径、Suppress 全都有），
**全局配置已经不是必需**。它唯一的作用：某个工程忘了放 `.clangd` 时，宏翻译还能少报几个错。

- **想彻底去掉**：删除 `%LOCALAPPDATA%\clangd\config.yaml` 即可，**已配置好的工程不受任何影响**（.clangd 完全自足）
- **保留也无害**：工具包 `2-自动配置.bat` 会生成它，4-一键检测 会检查它——保留就当兜底，删掉也不会报"配置缺失"之类的错
- 位置：`%LOCALAPPDATA%\clangd\config.yaml`（Windows 是 LOCALAPPDATA；Linux：`~/.config/clangd/`；macOS：`~/Library/Preferences/clangd/`）

（原内容保留作参考：匹配条件、宏、路径、Suppress 的写法都在下面，想恢复全局配置时照抄即可。）

```yaml
# ================= clangd 全局配置（C51 专用）=================
# 匹配条件：只有路径含 C51 文件夹的工程才应用本配置
# 原因：-Ddata= 等宏会污染其他工程（STM32 HAL 里有 data 变量名，会被宏吃成语法错误）
# 后果：51 工程请统一放在含 "C51" 的文件夹里，否则此配置不生效（需靠工程 .clangd 兜底）
If:
  PathMatch:
    - .*[/\\]C51[/\\].*

CompileFlags:
  Add:
    # 错误上限调大：必须！
    # 原因：被 Suppress 隐藏的误报仍会计入 clangd 错误上限（默认 19）；
    #       Keil REG52.H 有 54 条 "sbit X = P^N;" → 54 > 19 直接打爆，报 too many errors
    - -ferror-limit=100

    # ---------- C51 宏定义（消除 clangd 报错，不影响 Keil 编译）----------
    - -D__C51__            # 条件编译标识
    - -D__VSCODE_C51__     # 条件编译标识
    # Keil 存储模式/存储类型关键字 → 空宏（从源码中消失）
    - -Dreentrant=
    - -Dcompact=
    - -Dsmall=
    - -Dlarge=
    - -Ddata=
    - -Didata=
    - -Dpdata=
    - -Dbdata=
    - -Dxdata=
    - -Dcode=
    # Keil 扩展类型 → 映射成标准 C 类型（sbit LED=P2^0 → char LED=P2^0）
    - -Dbit=char
    - -Dsbit=char
    - -Dsfr=char
    - -Dsfr16=int
    - -Dsfr32=int
    # 函数式宏：处理带括号写法 _at_(0x30)；C51 无括号写法（interrupt 1）靠 Suppress 兜底
    - -Dinterrupt(x)=
    - -Dusing(x)=
    - -D_at_(x)=
    - -D_priority_(x)=
    - -D_task_(x)=

    # ---------- Keil 头文件路径（换机器只改这两行）----------
    - -ID:\Keil5\C51\C51\INC\Atmel   # REGX52.H（Atmel 版，sbit 是 0xNN 写法）在这里
    - -ID:\Keil5\C51\C51\INC        # reg51.h / REG52.H（Keil 版，sbit 是 P^N 写法）在这里
    # 工程头文件目录两种拼写都覆盖：inc/（简写）和 include/（全拼）
    # （目录不存在时静默忽略，无副作用；相对路径按工程根解析）
    - -Iinc
    - -Iinclude

Diagnostics:
  Suppress:
    - init_element_not_constant             # sbit X = P1^1 映射成 char 后"非编译期常量"误报
    - redefinition_different_typedef        # Keil stdio.h 的 size_t 与 clangd 内置头冲突（用 printf 才触发）
    - expected_fn_body                      # interrupt 1 / using 1（无括号写法）
    - invalid_token_after_toplevel_declarator  # _at_ 0x30（无括号写法）
```

**YAML 语法坑（写错整个配置不生效）**：`If` 必须放**文件顶部**；
数组用**块状写法**（`- xxx` 一行一个），不能写 `[.*[/\\]C51[/\\].*]` 这种嵌套方括号（解析报错）。

验证是否加载：`查看 → 输出` → 下拉选 clangd → 应有 `Loading config file at ...\clangd\config.yaml`。

### 3.2 项目 `.clangd`（每个 EIDE 工程一份，随 Git 走）

**为什么全局配置都有了，还要每个工程一份 `.clangd`？**

| 层 | 管什么 | 管不了什么 |
|---|---|---|
| 全局 config.yaml | C51 宏、Keil 头文件路径、抑制清单（机器层，所有工程共享） | **不知道每个工程自己的目录结构**（inc/ 还是 include/ 还是 App/），也不知道编译数据库在哪 |
| 项目 `.clangd` | 编译数据库位置（`CompilationDatabase`）+ 工程自己的 `-I`（工程层） | — |

机制（实测结论）：clangd 解析相对 `-I`（如 `-Iinc`）时，**按编译数据库的 directory 字段（=工程根目录）解析**。
没有 `.clangd` 指明数据库位置时，clangd 找不到 `build/Target 1/compile_commands.json`（它在子目录里，不会自动发现），
只能退回"按文件所在目录瞎猜"——`src/` 目录下的 main.c 找 `inc/` 就必然失败。
这就是 text 工程"inc 头文件全找不到"的直接原因：工程没有 `.clangd`。

**⚠️ 最容易写错的点（实测踩坑，满屏红的直接原因）**：`CompilationDatabase` 必须**缩进在 `CompileFlags:` 块下面**（两个空格），
**不能放顶层**。放顶层 clangd 报 `Unknown Config key 'CompilationDatabase'` 直接无视 → 找不到数据库 → 退回按文件目录瞎猜 → 满屏红。
判断方法：看 `CompilationDatabase` 这行前面有没有两个空格、`CompileFlags:` 是不是它的上级。验证命令：
`clangd --check=src/main.c`（在工程根目录跑）——输出 `All checks completed, 0 errors` 即正确，出现 `config warning ... Unknown Config key` 即写错。

**为什么 Keil 工程的编译数据库"又找得到又读不懂"**：EIDE 生成的 compile_commands.json 命令是 Keil 私有语法（`C51.exe ... INCDIR(...)`），
clangd 解析不了命令，但**它会用这条记录的 directory 字段（=工程根）**，再用 `.clangd` 的宏和 `-I` 重新合成可用命令。
所以 `.clangd` 的 `-Iinc` 等相对路径能正确解析——前提是它找到了数据库（也就是上一条：键名位置必须对）。

**CompilationDatabase 到底多重要（实测结论，别被"注释掉也能用"骗了）**：
- 两级目录工程（源码只在 `src\`、`inc\`）：`-I../inc` 在无数据库时也能兜底找到头文件 → **注释掉 CompilationDatabase 依然 0 红**（实测验证过）
- **源码嵌套更深（如 `src\app\`）**：`-I../inc` 失效（解析成 `src\inc`），此时**只有 CompilationDatabase 提供的工程根基准能让 `-Iinc` 正确工作**（实测：有它 0 错，注释掉 2 错）
- 结论：**保留它**（缩进正确 + 目标名对），它负责"多级目录 + 精确索引"；`-I../inc` 负责"没编译过时兜底"——两条各有各的用，不是重复。检测脚本对 CompilationDatabase 只报警告（两级目录实测能工作），但真踩到多级目录时它就是要命的那一行。

代价很小：一个隐藏文件。

**`.clangd` 完整内容（权威版本，与工具包「新建工程模板」里的文件逐字一致）**：
复制到工程根目录后**只需改一处**：
1. `CompilationDatabase: ./build/Target 1` → 与 eide.yml 里 `targets:` 的目标名一致（SDCC 模板工程是 `./build/Debug`）

（配置里没有任何工程专属路径——工程内头文件靠 `-Iinc` + `-I../inc` 两条互补相对路径，放哪个目录都通用；
只有换机器/换盘时改 Keil 那两行 `-ID:/Keil5/...`。）

```yaml
# ============================================================================
# .clangd —— 工程级 clangd 配置（每个 EIDE 工程根目录放一份，随 Git 走）
#
# 【它解决什么问题】为什么没有它就不行？
#   EIDE 生成的编译数据库（build\<目标名>\compile_commands.json）里的命令是
#   Keil 私有语法（C51.exe ... INCDIR(...)），clangd 读不懂。
#   这个文件负责告诉 clangd 四件事：
#     ① Keil 关键字怎么翻译成 clangd 能懂的形式（宏，见 CompileFlags.Add）
#     ② 头文件去哪找（-I 路径，见 CompileFlags.Add）
#     ③ 编译数据库在哪（CompileFlags.CompilationDatabase）
#     ④ 哪些 Keil 语法误报要压掉（Diagnostics.Suppress）
#
# 【生效方式】改完这个文件后：Ctrl+Shift+P → "clangd: Restart language server"
# 【自检命令】在工程根目录打开终端跑：clangd --check=src/main.c
#   看到 "All checks completed, 0 errors" = 配置正确
#   看到 "config warning ... Unknown Config key" = 有键名/缩进写错（见 CompilationDatabase 注释）
# ============================================================================

CompileFlags:
  # ---------- Add：给 clangd 追加的编译参数（宏翻译 + 头文件路径）----------

  Add:
    # 错误数量上限调大：Keil 的 REGX52.H 里有 54 行 "sbit xxx = Px^N;"，
    # 每一行都会产生一个被 Suppress 压掉的误报，而"被压掉的错误也计入上限"
    # （默认上限 19）→ 不调大就会整屏只显示 "too many errors" 一条。100 够用。
    - -ferror-limit=100

    # ---- 警告/误报全部压掉（Keil 代码风格与标准 C 不同，这些诊断没有参考价值）----
    # ⚠️ -Wno-main 必须有：Keil 允许 "void main()"，clang 默认会报
    #    "Return type of 'main' is not 'int'"（常见满屏红来源）
    - -Wno-main
    - -Wno-implicit-function-declaration  # 漏原型时 C99 会报 error，压掉（编译以 Keil 为准）
    - -Wno-implicit-int
    - -Wno-return-type
    - -Wno-unused-function
    - -Wno-unused-variable
    - -Wno-unused-parameter
    - -Wno-unused-label
    - -Wno-unused-value
    - -Wno-unknown-pragmas
    - -Wno-unknown-attributes
    - -Wno-unknown-argument
    - -Wno-incompatible-pointer-types
    - -Wno-builtin-declaration-mismatch
    - -Wno-format
    - -Wno-int-conversion
    - -Wno-enum-conversion
    - -Wno-macro-redefined
    - -Wno-sign-compare
    - -Wno-char-subscripts
    # ---- Keil C51 关键字 → clangd 能懂的形式（宏翻译）----
    # sbit / bit / sfr：Keil 特有类型，clangd 没有 → 用 char 承载。
    # 为什么用 char 而不是 _Bool（重要）：
    #   REGX52.H 里 "sfr P0 = 0x80;" 展开成 "char P0 = 0x80;" 是合法常量初始化；
    #   若用 _Bool，0x80 转 _Bool 不是编译期常量 → 报 init_element_not_constant /
    #   implicit-int。char 方案完全自足，不依赖全局配置
    #   （全局配置只在路径含 \C51\ 的文件夹才生效，工程挪到别处就失效）。
    - -Dsbit=char
    - -Dbit=char
    - -Dsfr=char
    # sfr16 / s16（16 位寄存器，如 T2）：同上，用 int 承载
    - -Dsfr16=int
    - -Ds16=int
    # code（程序存储器，如 "code unsigned char tab[]"）：内容是只读的 → const
    - -Dcode=const
    - -D__code=const
    # data / xdata / idata / pdata（存储类型限定符）：对 clangd 无意义 → 清空
    - -Ddata=
    - -Dxdata=
    - -Didata=
    - -Dpdata=
    # interrupt（中断函数）两种写法都要照顾：
    #   "interrupt 4"（无括号，Keil 常用）函数宏处理不了 → 靠下面的 Suppress 压掉
    #   "interrupt(4)"（带括号）→ 翻译成 __interrupt__(4)
    - -Dinterrupt(x)=__interrupt__(x)
    - -Dinterrupt=
    - -DINTERRUPT(x)=__interrupt__(x)
    # _at_（绝对定位，如 "unsigned char x _at_ 0x30;"）：翻译成属性
    - -D_at_=__attribute__((at))
    # using / REENTRANT 等 Keil 修饰符：清空即可
    - -D_using(x)=
    - -DREENTRANT=

    # ---- 头文件搜索路径（-I）----
    # Keil 标准头文件目录（REGX52.H、stdio.h 等）。
    # ⚠️ 换机器 / 换盘符时改这两行，其余内容不用动。
    - -ID:/Keil5/C51/C51/INC
    # Atmel 系列芯片头文件目录（AT89C52.H 等）
    - -ID:/Keil5/C51/C51/INC/Atmel
    # 工程自己的头文件目录。两种拼写都写：EIDE 模板和旧教程两种都用过，
    # 写全了不管工程用哪种都不会找不到。
    # 注意：工程没有 include/ 目录时这行会静默忽略（找不到的目录无副作用）。
    - -Iinc
    - -Iinclude
    # 互补路径（关键设计）：两条相对路径合起来覆盖两种解析模式——
    #   -Iinc    有编译数据库时按 directory（=工程根）解析 → 工程根\inc ✓
    #            （多级目录 src\app\main.c 也正确，因为基准是工程根）
    #   -I../inc 没有数据库（还没编译过）时按文件所在目录解析：
    #            src\main.c → src\..\inc = 工程根\inc ✓；inc\uart.c → 同理 ✓
    # 前提与局限：-I../inc 只对"源码在工程根直接子目录（src\、inc\）"有效；
    #            源码一旦嵌套更深（如 src\app\），它失效——此时靠上面的
    #            CompilationDatabase 提供工程根基准（见下方说明）。
    - -I../inc

  # ---------- Remove：从编译数据库命令里删掉的参数（Keil 命令行 clangd 不认）----------

  Remove:
    - -m*        # 内存模型参数（SMALL/LARGE 等）
    - -std=*     # 语言标准选择
    - -f*        # 编译器功能开关
    - -O*        # 优化参数
    - -g*        # 调试信息参数
    - -W*        # 警告参数（避免和上面 Add 里的 -Wno-* 冲突打架）
    - -D__C51__  # Keil 预定义宏
    - -D__CX51__ # Keil 预定义宏

  # ---------- CompilationDatabase：编译数据库位置（全文件最容易写错的地方！）----------
  #  1. 必须缩进在 CompileFlags: 下面（行首两个空格）——放顶层会被 clangd 当成
  #     未知键直接无视，日志报 "Unknown Config key 'CompilationDatabase'"。
  #  2. 路径 = build\<目标名>。目标名在 eide.yml 的 targets: 下面看：
  #     普通工程默认 "Target 1"，SDCC 模板是 "Debug"。写错 = 找不到数据库。
  #  3. 它到底有什么用（实测机制）：
  #     Keil 工程的编译数据库命令（INCDIR(...) 语法）clangd 解析不了，
  #     但它会采用记录里的 directory 字段（=工程根）作为相对路径基准，
  #     于是上面的 -Iinc 才能正确解析成 工程根\inc —— 多级目录（src\app\）
  #     全靠它。两级目录（src\、inc\）时 -I../inc 能兜底，注释掉这行也能工作，
  #     但一旦源码嵌套更深、或想索引更准，这行就是必需——所以保留它。
  CompilationDatabase: ./build/Target 1

Diagnostics:
  # ---------- Suppress：抑制 Keil 特有语法的误报 ----------
  # 注意：被压掉的报错仍然计入 ferror-limit，所以上面才要调到 100。
  Suppress:
    - expected_fn_body                        # "interrupt 1" 无括号形式产生的误报
    - invalid_token_after_toplevel_declarator # "_at_ 0x30" 无括号形式产生的误报
    - unknown_type                            # 未识别的类型名
    - unknown_typename                        # 同上（不同 clangd 版本用词不同，都写上）
    - redefinition_different_typedef          # Keil stdio.h 的 size_t 与 clang 内置类型冲突（printf 必触发）
    - init_element_not_constant               # "sbit x = P^N;" / "_at_ 0x30" 的非常量初始化误报
```

> 改动提醒：本文档与工具包「新建工程模板」里的 `.clangd` 是同一份内容，**改任何一处必须同步另一处**。

**这份配置包含什么（原理）**：

| 部分 | 干什么 |
|---|---|
| `-Dxxx=` 宏翻译（char 方案） | Keil 关键字（sbit/sfr/code/interrupt/_at_…）→ clangd 能懂的形式 |
| `-I` 路径（Keil 绝对 + 工程 `-Iinc`/`-I../inc` 互补相对路径） | 头文件去哪找；互补两条覆盖有/无编译数据库两种模式 |
| `-Wno-*` 全套 | 压掉 Keil 风格与标准 C 的差异诊断（含 `-Wno-main`：Keil 允许 `void main()`） |
| `CompileFlags.CompilationDatabase` | 编译数据库位置（必须缩进在 CompileFlags 下） |
| `Diagnostics.Suppress` 6 项 | 压掉 Keil 特有语法的误报（interrupt 无括号、size_t 冲突等） |

**为什么宏要用 char 方案、不能依赖全局配置**（实测教训）：全局 config.yaml 只在路径含 `\C51\` 的文件夹生效（PathMatch 规则）。
工程一旦放到别的路径（如 `D:\51code\`），全局配置整个失效——`.clangd` 必须**自足**。
另外 `-Dsfr=`（空定义）会让 `sfr P0 = 0x80;` 展开成 `P0 = 0x80;` 触发 implicit-int；
`-Dsfr=char` 展开成 `char P0 = 0x80;` 才是合法声明。REGX52.H 位于 `D:\Keil5\C51\C51\INC\Atmel\`（不在 INC 根目录），所以 Atmel 路径必须写。

**为什么 `-I` 要写两条互补相对路径 + 全套 `-Wno-*`（实测必成）**：

| 场景 | 只有 `-Iinc` | 加上 `-I../inc`（互补） |
|---|---|---|
| 编译过（有 DB） | `-Iinc` 按 directory（=工程根）解析 ✓ | ✓ |
| **没编译过（无 DB）** | fallback 按**文件所在目录**解析：src\ 下的 main.c 找 src\inc → **`'uart.h' file not found` + 满屏连锁红** | `-I../inc`：src\main.c → src\..\inc = 工程根\inc ✓ |
| 工程在非 C51 路径（全局配置失效） | 宏/Suppress 缺失 → implicit-int 等 | 自足 ✓ |
| `void main()` | 报 `Return type of 'main' is not 'int'` | `-Wno-main` 压掉 ✓ |

（`-Iinc` 和 `-I../inc` 各在一个模式下失效，但失效的那个只是"找不到目录"被静默忽略，另一个生效——两条合起来覆盖全部场景，且**全是相对路径，零写死**，工程放哪、叫什么名字都不影响。前提：源码放在工程根的直接子目录 `src\` 和 `inc\`，不要嵌套更深。）

**操作顺序（重要）**：改完 `.clangd` 后必须 `Ctrl+Shift+P` → `clangd: Restart language server`，
否则 clangd 一直用旧配置（旧诊断会留在"问题"面板里，看起来像没修好）。

### 3.3 源码编码：必须 UTF-8 无 BOM

- **原因**：clangd 只认 UTF-8，GBK 源码每个中文注释都是 invalid UTF-8 错误；
  而**带 BOM 也不行**——Keil C51 编译器把 BOM 当非法字符，第一行 `#ifndef` 失效（实测 8 个编译错误）。
- **不做会怎样**：新工程（江协模板/普中例程都是 GBK）打开后红波浪成片、补全跳转失效。
- **操作**：新工程若是 GBK，在工程根目录运行：

```powershell
$gbk = [System.Text.Encoding]::GetEncoding(936)
$utf8 = [System.Text.UTF8Encoding]::new($false)   # $false = 不带 BOM（带 BOM 会编译报错）
Get-ChildItem -Recurse -File | Where-Object { $_.Extension -in '.c','.h' } | ForEach-Object {
  $b = [System.IO.File]::ReadAllBytes($_.FullName)
  try { $null = [System.Text.UTF8Encoding]::new($false,$true).GetString($b); return } catch {}
  [System.IO.File]::WriteAllText($_.FullName, $gbk.GetString($b), $utf8)
}
```

（VSCode 显示层有 autoGuessEncoding 兜底，但 clangd 不吃这套，必须转。）

---

## 第 4 步：打开工程

- **51 工程建议放路径含 `C51` 的文件夹**（如 `D:\Users\Documents\C51\`）：
  - 原因：以前是为了让全局配置生效（PathMatch 规则）；**现在每个工程自带自足的 `.clangd`，放哪里都能用**，放 C51 文件夹只是保持习惯、方便管理。
- **Keil 换了路径**：EIDE 会找不到工具链 → 设置 `EIDE.C51.INI.Path` 填新路径（TOOLS.INI 或 UV4.exe 都认）→ 重载窗口。
- 新增/删除源文件后**重新编译一次**（compile_commands 才更新）；改了配置后 `clangd: Restart language server`。

---

## 第 4.5 步：EIDE 新建工程 / 从 Keil 迁移

### A. 新建工程四步（每步：操作 → 为什么 → 验证）

> **为什么新建的工程一定报错**：SDCC 模板（向导里带 SDCC 字样的选项）生成的工程默认有 4 个坑：
> ① 工具链是 SDCC（机器上没装）② `srcDirs` 只有 src（inc 里的 .c 不编译）③ 「包含目录」只有 inc（REGX52.H 在 Atmel 子目录里）④ 没有 `.clangd`（编辑器满屏红）。
> 下面四步正好填平这 4 个坑，做完 = 编译 0 错误 + 编辑器 0 红。

**第 1 步：切工具链**（解决"无法找到编译器 SDCC"）
- 操作：EIDE 面板 → 右键**最顶层的工程名** → 项目设置 → **工具链** → 选 **Keil_C51**
- 为什么：模板默认工具链是 SDCC，而机器上没装 SDCC，一编译就报"无法找到编译器"；切到 Keil_C51 后编译才调用 `D:\Keil5\C51\C51\BIN\C51.exe`
- 验证：编译日志第一行出现 `C51 COMPILER V9.60.7.0`

**第 2 步：注册源文件目录**（解决 `C Files: 0/1` + 链接 `L127`）
- 操作：右键工程名 → **添加目录** → 依次选 `src`、`inc`
- 为什么：EIDE 只编译"注册过"的目录里的文件。`uart.c` 放在 `inc\` 里，不注册 inc → 它不参与编译 → main.c 调用的 `uart_init`/`rx_byte` 等全部报 `L127 UNRESOLVED EXTERNAL`（还有 `?C_START`——一个 C 文件都没有时运行时库不会链进来）
- 验证：编译日志第一行表格 `C Files` = 你的 .c 总数（main.c + uart.c = 2）

**第 3 步：加「包含目录」**（解决 `can't open file`，最容易加错的一步）
- 操作：右键工程名 → 项目设置 → 找到 **「包含目录」**（⚠️ 不是「库搜索目录」！那是给链接器找 .lib 库文件的，头文件是编译器的事，加那里无效）→ 添加两行：
  - `inc`：让 `src\main.c` 里的 `#include "uart.h"` 能找到（头文件在 `inc\`）
  - `D:/Keil5/C51/C51/INC/Atmel`：`REGX52.H` 住在 `INC\Atmel\` 子目录（INC 根目录只有 REG51.H/REG52.H），不加它 `#include <REGX52.H>` 必报 `can't open file 'REGX52.H'` + 16 个 undefined（SCON/TMOD/TH1/SBUF 全是连锁）
- 验证：编译日志 C51.exe 那行 `INCDIR(...)` 里出现 `Atmel` = 加对了
- 注意：Keil 标准 INC 根目录（REG51.H、stdio.h）EIDE 会自动附加到每个 Keil_C51 工程，不用手动加

**第 4 步：放 `.clangd`**（解决编辑器满屏红）
- 操作：
  1. 把工具包「新建工程模板」里的 `.clangd` 复制到**工程根目录**（顺手把同目录的 `.clang-format` 也复制过去——它管"保存时自动格式化"的风格，没有它 clangd 会用默认 LLVM 风格把你的代码大改一遍）
  2. 改一处：`CompilationDatabase: ./build/Target 1` → **`./build/Debug`**（SDCC 模板新建的工程目标名是 Debug；普通空工程才是 Target 1——看 eide.yml 里 `targets:` 下面那行确认）
  3. `Ctrl+Shift+P` → **`clangd: Restart language server`**（必须做！不重启，旧红波浪永远留在问题面板里）
- 为什么：EIDE 生成的编译数据库命令是 Keil 私有语法（`INCDIR(...)`），clangd 读不懂；`.clangd` 负责告诉 clangd：Keil 关键字怎么翻译（宏）、头文件去哪找（-I）、数据库在哪（CompilationDatabase）、哪些误报要压（Suppress）——完整原理见 3.2 节
- 验证：打开 `main.c` 无红波浪

**最终验证**：`Ctrl+Shift+B`，三条全过才算成功：

```
C51 COMPILATION COMPLETE.  0 ERROR(S)
LX51 RUN COMPLETE.  0 WARNING(S),  0 ERROR(S)
[ DONE ] build successfully !
```

烧录文件在 `build\Debug\<工程名>.hex`。

### B. 报错速查表

| 报错 / 现象 | 原因 | 处理 |
|---|---|---|
| "无法找到编译器 SDCC，需要安装吗？" | 工具链还是 SDCC | 第 1 步 |
| `C Files: 0/1` + `L127 UNRESOLVED EXTERNAL`（?C_START） | 源文件目录没注册 | 第 2 步 |
| `can't open file 'REGX52.H'` + 16 个 undefined（SCON/TMOD/SBUF…） | 包含目录缺 Atmel | 第 3 步 |
| `can't open file 'uart.h'` + 连锁 undefined | 包含目录缺 inc | 第 3 步 |
| `'uart.h' file not found` / `Return type of 'main' is not 'int'` | `.clangd` 缺失、路径没改、或没重启 clangd | 第 4 步 |
| 编译 0 错但编辑器满屏红 | 没有 `.clangd` / 源码还是 GBK | 第 4 步；编码见 3.3 |
| `ERROR C202：xxx undefined` | 头文件/宏没找到 | 第 3 步 + 项目设置宏定义（defineList） |
| `ERROR C249: SEGMENT TOO LARGE` | 内存模型 SMALL 太小 | 项目设置 ram-mode 改 LARGE |
| `RESTRICTED` / `CODE SIZE LIMIT EXCEEDED` | 评估版许可证限 2KB 代码 | Keil → File → License Management 激活 |
| `LICENSE ERROR (R207)` | 评估版警告，**无害**，忽略 | — |
| 烧录 `PermissionError(13)` | 串口被监视器占用 | 先关 Serial Monitor 再烧 |

### C. 界面名词对照（别找错）

| 中文界面 | 英文 | 管什么 |
|---|---|---|
| **包含目录** | Include Paths | 编译器找 `.h` 头文件 ← 头文件路径加这里 |
| **库搜索目录** | Library Paths | 链接器找 `.lib` 库文件 ← 别把头文件加这里 |
| 项目设置 | Project Settings | 右键工程名进入（工具链、包含目录、芯片都在里面） |
| 添加目录 | Add Directory | 把文件夹注册进工程（该目录下的 .c 参与编译） |
| 工具链 | Toolchain | 选编译器：Keil_C51 / SDCC |
| 目标名 | Target | 决定编译数据库路径：普通工程 `Target 1`，SDCC 模板 `Debug`（看 eide.yml `targets:`） |
| 芯片 | Device | 型号识别（可选，不影响编译） |

### D. 从 Keil 工程迁移到 EIDE

**方法**：EIDE 面板 → 导入 Keil 工程 → 选 `.uvproj`。

**迁移后要做的事（实测：跟新建工程大差不差，就是 A 节四步里的第 2、3、4 步 + 编码）**：

1. **补目录注册 + 包含目录**（对应 A 节第 2、3 步）
   - 操作：右键工程名 → **添加目录** → 把所有含 `.c` 的目录（src、inc…）都选上；
     右键工程名 → 项目设置 → **包含目录** → 检查 Keil 导入的路径是否有效（Keil 的 Include Paths 会导入，但**相对路径的基准变了、绝对路径要看目标目录还在不在**），缺的补上：`inc`、`D:/Keil5/C51/C51/INC/Atmel`
   - 为什么：EIDE 导入不等于全部注册成功——源文件树要确认、包含目录要核对，漏一个就是 C Files 少 / `can't open file`（对照 B 表）
   - 验证：编译日志 `C Files` 数量 = 你的 .c 总数；`INCDIR(...)` 里出现 Atmel

2. **放 `.clangd`，基准目录（目标名）自己改**（对应 A 节第 4 步，⚠️ 与新建的差异点）
   - 操作：复制工具包「新建工程模板」的 `.clangd` 到工程根目录；
     **先看 eide.yml 里 `targets:` 下面那行**（Keil 导入的工程目标名继承自 .uvproj，可能是 `Target 1`，也可能是别的名字），
     再把 `.clangd` 里的 `CompilationDatabase: ./build/Target 1` 改成 **`./build/<你工程的目标名>`**
   - 为什么：编译数据库生成在 `build\<目标名>\` 下，**基准目录写错 = clangd 找不到数据库 = 满屏红**。新建工程的目标名就两种（Target 1 / Debug），导入工程的目标名随 .uvproj 变，所以必须自己看一眼再改
   - 验证：把工具包「检测工程配置.bat + .ps1」复制进工程跑一遍（它会自动读出目标名并指出 .clangd 该改成什么；答 y 可直接交互修正）

3. **编码 + 杂项检查**
   - **编码**：Keil 源码几乎都是 GBK → 转 UTF-8 无 BOM（`3-编码转换.bat`，见 3.3）
   - **宏定义**：条件编译依赖的宏必须补（如 `XTAL=11059200`）——Keil 的宏会导入，核对 defineList
   - **芯片型号**：导入后常为空 → 项目设置里选
   - **内存模型**：SMALL/LARGE 与 Keil 原工程一致
   - **STARTUP.A51**：保留在工程里（C51 启动代码）
   - **烧录器**：Keil 工程没有烧录配置 → 配 stcgal（见第 5 步）

> 迁移原则：**Keil 能编译的工程，EIDE 一定能编译**——它只是换壳调同一个 C51.exe。
> 报错九成出在：编码、包含目录、宏定义、文件注册四件事，按 B 表对号入座。
> 迁移后别手测，直接把「检测工程配置」复制进去跑一遍，哪里缺补哪里。

### E. 为什么模板总有这些坑（根因，懂了才能自己排查）

EIDE「新建项目」向导里带 SDCC 字样的选项（如 8051 Quick Start）会下载 SDCC 模板：工具链 SDCC、`srcDirs` 只有 src、「包含目录」只有 inc、没有 `.clangd`——4 个坑叠加，所以每次新建都失败，**不是你的操作问题**。
（text 工程曾用 EIDE 构建日志实锤过：incList 缺 inc 时 `#include "uart.h"` 报 `C318 can't open file` + 5 个连锁 ERROR。）

> 备选：不想每次走四步，可直接复制工具包「新建工程模板」文件夹改名即用（已实测通过）。

---

## 第 5 步：EIDE 烧录（一键下载，替代 STC-ISP）

- **原因**：默认烧录方式是弹 STC-ISP 外部程序，要手动选 hex、手动点下载；stcgal 让烧录全程留在 VSCode。
- **不做会怎样**：每次烧录都要切软件，调试循环变慢。
- **操作**：
  1. `pip install stcgal`（Python 已装；验证：`stcgal --version`）
  2. EIDE 面板 → **烧录器 (Flasher)** → 选 **stcgal** → 填：
     - 串口：插板后到**设备管理器**确认（普中/江协板是 CH340，如 COM3）
     - 芯片型号：如 `STC89C52RC`（stcgal 用 stc89a 协议；`auto` 偶尔握手失败，建议显式选）
     - 波特率：115200
     - 保存（生成 `.eide/stc.flash.json`）
  3. 芯片型号同时在 EIDE 项目信息里设置（当前 eide.yml 的 deviceName 是 null，烧录型号识别靠它）
  4. 烧录流程：**先关 Serial Monitor** → `Ctrl+Shift+B` → `build` → `flash` → 提示时**给板子断电再上电**
- **两个原理性提醒**：
  - **为什么必须先关串口监视器**：Windows 串口是独占的，监视器占着 COM3，stcgal 打开端口直接
    `PermissionError(13)` 拒绝访问。
  - **为什么要断电上电**：STC 芯片是"冷启动下载"——只有上电瞬间才检测下载指令；烧录时会提示重新上电，照做即可。

---

## 第 6 步：串口调试（printf 打印）

- **原因**：51 没有硬件断点调试，printf + 串口是唯一调试手段。
- **操作**：`Ctrl+Shift+P` → `Serial Monitor: Focus on Serial Monitor View` → 选 COMx + 波特率。
- **51 串口能发数据的三个前提**（缺一不可）：
  1. 初始化：`SCON=0x50`（方式1，允许接收）+ 定时器1方式2
  2. 波特率正确：`9600 = 11059200 / 12 / 32 / (256 - TH1)` → `TH1=0xFD`（11.0592MHz，12T）
  3. **printf 重定向**：Keil 的 `printf()` 最终调用 `putchar()`，实现它即可：

```c
char putchar(char c) {          // 放在 uart.c 里（需包含 reg52.h / REGX52.H）
    uart_send_byte((unsigned char)c);
    return c;
}
```

- **现成模块**：工具包「新建工程模板」的 `inc/uart.c` + `inc/uart.h`（9600 串口模块：中断收发 + putchar 重定向 + 串口回显测试 main，复制进工程即可用，main 里 `uart_init();` 一行启用）。
- **排错**：乱码=波特率不匹配；没输出=没烧录成功/没复位；烧录失败=监视器没关。

---

## 第 7 步：STM32 阶段（预配置，大二用）

- **原因**：你有两套 Keil（C51 + MDK，见第 0.5 步），EIDE 按工程类型自动选工具链，互不干扰——
  提前配好 ARM 路径，大二开 STM32 工程零配置。
- **操作（推荐自动化）**：运行工具包 `2-自动配置.bat`——自动检测 C51/MDK 并写入全部 EIDE 设置。
  手动填写时按此表：

| 设置项 | 值（按实际路径） |
|---|---|
| `EIDE.ARM.INI.Path` | `D:\Keil5\MDK\TOOLS.INI` |
| `EIDE.ARM.ARMCC6.InstallDirectory` | `D:\Keil5\MDK\ARM\ARMCLANG`（可执行文件是 armclang.exe） |
| `EIDE.ARM.GCC.InstallDirectory` | 可选，**未安装可留空**；需要时让 EIDE 自动下载或 `winget install Arm.GnuArmEmbeddedToolchain` |

- **大二流程**：CubeMX 生成 HAL 工程 → EIDE 导入 → AC6 编译 → 烧录选 **ST-Link**（驱动随 MDK）
  或 **OpenOCD**（EIDE 可自动下载）→ 调试装 cortex-debug + mcu-debug 全家扩展。
- **注意**：armclang 编译需要 MDK 许可（Keil 界面能编译即正常）；换电脑记得激活。

---

## 第 8 步：验证（按需检测，每项原理清楚）

**方式**：双击工具包 `4-一键检测.bat` → 交互菜单输入编号（如 `1,4,7`，回车=全部）；
或命令行 `powershell -File scripts\4-verify.ps1 -Only "1,4,7"` 指定检测项。

| 编号 | 检测项 | 检测原理（为什么能证明配置正确） |
|---|---|---|
| 1 | 扩展（13 个） | 扩展安装后会在 `%USERPROFILE%\.vscode\extensions\` 生成对应文件夹，检查文件夹是否存在即判定安装状态 |
| 2 | 用户设置 | 检查 settings.json 关键键值：autoGuessEncoding（编码识别）、clangd.arguments（引擎参数）、renderWhitespace（空格显示）、EIDE 各项 |
| 3 | 快捷键 | keybindings.json 是否包含 Ctrl+Alt+L 绑定 |
| 4 | 全局 clangd 配置 | clangd 在 Windows **固定读取** `%LOCALAPPDATA%\clangd\config.yaml`，检查文件存在且含 PathMatch / -I / Suppress / ferror-limit 四个关键段 |
| 5 | 工具链 | Python/stcgal/Git 装好后在 PATH 留下命令，Get-Command 能查到即已安装 |
| 6 | Keil 路径 | 从 EIDE 设置（TOOLS.INI 的 [C51] PATH 或 UV4.exe 目录推导）→ 扫 6 个常见位置兜底 → 验证 C51.exe/armclang.exe **真实存在** |
| 7 | clangd 代码检测 | 用 clangd `--check` 对检测工程 main.c 做真实语法分析；该文件故意含 sbit/_at_/interrupt/stdio 全部疑难语法——**0 诊断 = 宏 + 头文件路径 + 抑制清单全生效** |
| 8 | Keil 编译检测 | 直接调用 C51.exe 真实编译检测工程 main.c（0 错误=通过）。比 clangd 更严格：验证真实工具链全链路（只编译不链接，链接由 EIDE 做） |

> 日常使用小验证（不用跑脚本）：`main.c` 输入 `uns` 弹 `unsigned` = clangd 通；
> `Ctrl+Shift+B` 出 hex = 编译通；断电上电烧录成功 = 烧录通；串口看到打印 = 调试通。

---

## 同步策略（换机器不再重配）

| 内容 | 同步方式 | 原因 |
|---|---|---|
| 用户设置/快捷键/扩展清单 | **Settings Sync**（登录账号自动同步 + 自动装扩展） | VSCode 官方机制，管"机器层" |
| 项目文件（.clangd、tasks.json、.eide/、源码） | **Git**（clone 即得） | Settings Sync 不碰项目文件 |
| 全局 config.yaml（3.1） | 由 2-自动配置.bat 重新生成 | 机器级文件，无法同步；脚本自动检测 Keil 路径 |
| Keil / Python / Git 本体 | 各自安装 | 系统软件，装到与配置相同的路径则零修改 |

**新机器 5 分钟流程**：装 VSCode + Keil（双版本，见 0.5 步）+ Python + Git → 登录 VSCode
（设置/扩展自动恢复）→ `git clone` 工程 → **运行工具包 `2-自动配置.bat`**
（已知目录可直接输入跳过搜索；一站式写入 51+STM32 全部 EIDE 设置并生成 clangd 配置）
→ 打开工程 → **`4-一键检测.bat`** 按需验证全绿。
